import { PageShell } from '@/components/page-shell';

export default function SearchPage() {
  return (
    <PageShell
      title="Search"
      description="Find products, collections and custom options across the catalog."
    />
  );
}
