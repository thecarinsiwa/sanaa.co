import { PageShell } from '@/components/page-shell';

export default function AccountPage() {
  return (
    <PageShell
      title="Sign in"
      description="Access your orders, measurements and customization requests."
    />
  );
}
